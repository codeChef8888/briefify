# Tests

This directory is reserved for automated tests (unit, integration, contract).

Guidelines:
- Place fast unit tests under `tests/unit`.
- Place integration tests under `tests/integration`.
- Keep ad-hoc/manual verification scripts under `scripts/manual`.
